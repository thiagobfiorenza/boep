<h1>New category</h1>

<% form_for(@category) do |f| %>
  <%= f.error_messages %>

	<%= render :partial => 'categories_form', :locals => {:f => f} %>

  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', categories_path %>
