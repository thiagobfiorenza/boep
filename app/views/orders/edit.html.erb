<h1>Editing order</h1>

<% form_for(@order) do |f| %>
  <%= f.error_messages %>

	<%= render :partial => 'orders_form', :locals => {:f => f} %>

<% end %>

<%= link_to 'Show', @order %> |
<%= link_to 'Back', orders_path %>
