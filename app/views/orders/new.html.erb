<h1>New order</h1>

<% form_for(@order) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'orders_form', :locals => {:f => f} %>

  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', orders_path %>
