<h1>New unit</h1>

<% form_for(@unit) do |f| %>
  <%= f.error_messages %>

	<%= render :partial => 'units_form', :locals => {:f => f} %>

  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', units_path %>
